{
  "name": "apply-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"const fs=require('fs');const path=require('path');if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true});fs.mkdirSync('dist');['popup','lib','data','manifest.json'].forEach(f=>fs.cpSync(f,path.join('dist',f),{recursive:true}))\"",
    "test": "node --test tests/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test tests/*.test.mjs",
    "test:e2e": "playwright test --config config/playwright.config.mjs",
    "lint": "eslint popup/popup.js lib/*.js --max-warnings=0 --config config/eslint.config.mjs"
  },
  "dependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^10.5.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.1",
    "@playwright/test": "1.61.0",
    "jsdom": "^29.1.1"
  }
}
